{
  "name": "ynu-website",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "postinstall": "npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  }
}